{
  "name": "hazardhub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'no build step' || true",
    "deploy": "netlify deploy --build --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "undici": "^6.19.8"
  }
}